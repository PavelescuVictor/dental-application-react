import styled, { css } from 'styled-components';

const StyledAddOrderStatus = styled.div`
  ${() => css``}
`;

StyledAddOrderStatus.displayName = 'StyledAddOrderStatus';

export default StyledAddOrderStatus;
