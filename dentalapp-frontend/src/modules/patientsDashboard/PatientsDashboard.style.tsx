import styled, { css } from 'styled-components';

const StyledPatientsDashboard = styled.div`
  ${() => css``}
`;

StyledPatientsDashboard.displayName = 'StyledPatientsDashboard';

export default StyledPatientsDashboard;
