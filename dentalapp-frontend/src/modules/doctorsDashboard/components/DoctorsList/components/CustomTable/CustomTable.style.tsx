import styled, { css } from 'styled-components';

const StyledCustomTable = styled.div`
  ${(props) => {
    const {
      theme: { palette },
    } = props;
    return css``;
  }}
`;

StyledCustomTable.displayName = 'StyledCustomTable';

export default StyledCustomTable;
