import styled, { css } from 'styled-components';

const StyledAddOrderColor = styled.div`
  ${() => css``}
`;

StyledAddOrderColor.displayName = 'StyledAddOrderColor';

export default StyledAddOrderColor;
