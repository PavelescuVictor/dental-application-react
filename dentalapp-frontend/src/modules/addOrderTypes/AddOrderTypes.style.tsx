import styled, { css } from 'styled-components';

const StyledAddOrderTypes = styled.div`
  ${() => css``}
`;

StyledAddOrderTypes.displayName = 'StyledAddOrderTypes';

export default StyledAddOrderTypes;
