import styled, { css } from 'styled-components';

const StyledAdminDashboard = styled.div`
  ${() => css``}
`;

StyledAdminDashboard.displayName = 'StyledAdminDashboard';

export default StyledAdminDashboard;
