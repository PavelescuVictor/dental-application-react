import StyledLoader from './Loader.style';

const Loader = (): JSX.Element => {
  console.log('Loader');
  return (
    <StyledLoader>
      <p>Loader</p>
    </StyledLoader>
  );
};

export default Loader;
