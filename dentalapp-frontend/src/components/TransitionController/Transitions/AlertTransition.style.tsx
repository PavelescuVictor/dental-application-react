import styled, { css } from 'styled-components';

const AlertTransition = styled.div`
  ${(props) => {
    const { theme: palette } = props;
    return css``;
  }}
`;

AlertTransition.displayName = 'AlertTransition';

export default AlertTransition;
